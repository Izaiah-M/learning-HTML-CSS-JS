import domFuntions from "./learning";
// Or import { default as domFunctions } from '../modules/dom-functions.js';
const { toggleHiddenElement, test } = domFuntions; // this is for when the default export is an object
